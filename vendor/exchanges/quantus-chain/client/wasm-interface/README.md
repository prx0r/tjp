# sp-wasm-interface (vendored)

Vendored from crates.io `sp-wasm-interface` **24.0.0**, then patched so the
optional `wasmtime` dependency is **36.0.13** (was 35.0.0) and unifies with the
patched `sc-executor-wasmtime`.

License: Apache-2.0
